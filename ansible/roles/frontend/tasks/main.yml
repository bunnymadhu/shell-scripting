# - name: Print Component Name
#  debug:
#   msg: Component = frontend

- name: Install Nginx
  yum:
    name: nginx
    state: present

- name: Start Nginx
  ansible.builtin.systemd:
    state: started
    name: nginx
    enabled: yes